%% 可以计算所有点local reachability density

%最受欢迎点的欢迎度
% max_nb = max(Rnbi)
% 
% lrds = zeros(DataSet.n,1);
% for p = 1:1:DataSet.n
%     neighbors = NN(DataSet,max_nb,p);
%     [lrd] = LRD(DataSet,p,max_nb,neighbors);
%     lrds(p) = lrd;
% end

%% 